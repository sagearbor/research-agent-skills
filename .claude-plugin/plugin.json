{
  "name": "research-skills",
  "version": "1.2.0",
  "description": "Self-authored Claude Code skills with versioned regression tests and a passive model-telemetry matrix: llm-usage-ledger (org-wide LLM usage accounting \u2014 cloud API, subscription ingest, local models \u2014 with price series and $-vs-tokens dashboards; no local-model software required), md-convert (markdown to docx/html/pdf/xlsx with Word-native TOC), lmstudio-panel (local LM Studio server control, delegates accounting to llm-usage-ledger), meeting-canvas (interactive planning worksheets), sage-agent-tempo (agent work tracking), pub-readiness-check (publication readiness audit), codex-security-scan (Claude-driven security scanning on OpenAI's open-source Codex Security methodology \u2014 no OpenAI key \u2014 with a cross-repo findings workbench, SARIF, and an HTML dashboard), secret-guard (blocks credentials reaching git \u2014 PreToolUse hook needs no setup, plus a global git hook; no API key, no model call), azure-credential-guard (stops DefaultAzureCredential hanging for minutes off-Azure while keeping managed identity working on Azure \u2014 offline, stdlib-only tests, no Azure account needed).",
  "author": {
    "name": "sagearbor"
  },
  "repository": "https://github.com/sagearbor/research-agent-skills"
}
